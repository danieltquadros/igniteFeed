export interface model {
  sendFeedback: string;
  placeholderComment: string;
  requiredField: string;
  publishTextButton: string;
}

export const defaultInstance = {
  sendFeedback: "",
  placeholderComment: "",
  requiredField: "",
  publishTextButton: "",
};
